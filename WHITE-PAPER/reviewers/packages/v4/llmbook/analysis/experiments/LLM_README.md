# 6_EXPERIMENTS

Suggested experiments and validation ideas generated during `--full` ingestion mode.

## Purpose

When `ingest.py --ingest --full` is run, Claude proposes:
- Specific follow-up experiments
- Validation protocols for claims
- Replication suggestions
- Edge cases to investigate

## Generation

This directory is populated by Claude during full ingestion review.
Content here directly feeds into experiment planning (S7 ARMADA runs).

---
*Part of the LLM_BOOK ingestion pipeline*
*Generated content requires --full flag*
