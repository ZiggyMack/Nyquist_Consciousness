<!-- FROSTY_MANIFEST
last_reviewed: 2025-12-17
depends_on:
  - ../BRIDGE/README.md
  - ../BRIDGE/scripts/left/
impacts:
  - galleries/
  - extractions/
keywords:
  - extraction
  - proof
  - analytical
  - statistical
-->

# LEFT HEMISPHERE

**Structured • Analytical • Rigorous • Sequential**

---

## Neuroscience-Aligned Role

LEFT is the **formalization hemisphere**.

It does not own all truth, all math, or all evidence. Its job is to turn signal into explicit, checkable structure:

- names
- definitions
- ordered claims
- metric domains
- evidence anchors
- caveats
- contradiction notes

Practical rule:

```text
LEFT names, sequences, and verifies the shape.
```

See `../BRIDGE/docs/HEMISPHERE_MODEL.md` for the full LEFT / RIGHT / BRIDGE doctrine.

---

```
    ╔═══════════════════════════════════════╗
    ║                                       ║
    ║   🧠 THE SCIENTIST'S VIEW            ║
    ║                                       ║
    ║   Facts. Tables. Logic. Evidence.    ║
    ║   Step by step. Prove everything.    ║
    ║                                       ║
    ╚═══════════════════════════════════════╝
```

---

## What Lives Here

The LEFT hemisphere contains:

| Directory | Purpose |
|-----------|---------|
| `galleries/` | Concept files with structured presentation |
| `extractions/` | Tagged, categorized data from experiments |
| `data/` | Processed, normalized datasets |
| `visualizations/` | Charts, graphs, statistical plots |

---

## The LEFT Brain Approach

When processing consciousness research through the LEFT hemisphere:

1. **Define** - Clear terminology, precise definitions
2. **Measure** - Quantitative metrics, statistical tests
3. **Categorize** - Organize into taxonomies
4. **Prove** - Hypothesis testing, p-values, effect sizes
5. **Document** - Tables, structured markdown, citations

---

## LEFT Acceptance Criteria

A durable LEFT file should make it easy for another agent to check the claim without guessing.

Before a concept is treated as current, include:

- the exact claim being made
- source signal: run ID, sync file, map, paper, or pipeline batch
- metric domain: `cosine-current`, `legacy-rms`, `mixed`, or `non-metric`
- current vs historical status
- caveat or boundary condition
- link to the paired RIGHT file when it exists

If those pieces are missing, mark the concept as `seed` or `needs-refresh`.

---

## Gallery Structure

```
LEFT/galleries/
├── validated/       # Empirically confirmed (with statistics)
├── foundations/     # Core theory (with formal definitions)
├── speculative/     # Hypotheses (with testable predictions)
└── frontiers/       # Active research (with methodology)
```

Each concept file emphasizes:
- Tables and structured data
- Statistical evidence
- Logical progression
- Academic rigor

---

## Tools (via BRIDGE)

The LEFT hemisphere uses analytical tools from `BRIDGE/scripts/left/`:

- `run_extraction.py` - Extract and categorize data
- `compute_statistics.py` - Statistical analysis
- `validate_hypothesis.py` - Formal hypothesis testing

---

## Connection to RIGHT

The LEFT hemisphere analyzes what the RIGHT hemisphere intuits.

```
RIGHT (intuition) → BRIDGE (integration) → LEFT (verification)
LEFT (evidence) → BRIDGE (integration) → RIGHT (synthesis)
```

Neither hemisphere is complete without the other.

---

**"In God we trust. All others bring data."**
